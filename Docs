Haz commit:
> Add System Brief with context diagram

⚠ GitHub renderiza Mermaid automáticamente.  
Si lo ves como gráfico → está perfecto.

---

# 🟦 PASO 5 — requirements.md

Copia esto:

```markdown
# Requirements

## Backlog Board

(Pegar aquí el link de tu Trello)

---

## Historias de Usuario

### HU1 – Registrar paquete (Must)

Como administrador  
Quiero registrar un paquete  
Para incluirlo en la planificación de rutas  

**Criterios (Given/When/Then):**

Given que el administrador está autenticado  
When registra un paquete válido  
Then el sistema lo guarda correctamente  

---

### HU2 – Calcular ruta óptima (Must)

Como administrador  
Quiero calcular la ruta óptima  
Para reducir costos  

**Criterios (Given/When/Then):**

Given que existen paquetes registrados  
When selecciono "Calcular ruta"  
Then el sistema genera la ruta más corta  

---

### Otras historias

- HU3 – Visualizar ruta (Must)
- HU4 – Actualizar estado (Should)
- HU5 – Registrar repartidor (Should)
- HU6 – Historial (Could)
- HU7 – Reporte (Could)
- HU8 – GPS en tiempo real (Won’t)

---

## MVP Rationale

El MVP incluye las historias HU1, HU2 y HU3,
ya que representan el núcleo funcional del sistema.
Sin el registro de paquetes ni el cálculo de rutas,
el sistema no genera valor operativo.
Las demás funcionalidades agregan valor incremental
pero no son esenciales para la primera versión.
